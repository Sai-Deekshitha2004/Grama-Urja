<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/window_bg">

    <!-- Top Header -->
    <View
        android:id="@+id/headerBg"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="@color/primary_dark"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/welcomeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="30dp"
        android:text="Namaste, Farmer"
        android:textColor="@color/white"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/btnEditProfile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="20dp"
        android:background="?attr/selectableItemBackground"
        android:padding="8dp"
        android:text="Edit Profile"
        android:textColor="#BBDEFB"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/welcomeText"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/welcomeText" />

    <!-- Zone Selection Card -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/zoneCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="-30dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="8dp"
        app:layout_constraintTop_toBottomOf="@id/headerBg">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="15dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Monitoring Zone"
                android:textColor="@color/text_sub"
                android:textSize="12sp"
                android:textAllCaps="true"
                android:letterSpacing="0.1" />

            <Spinner
                android:id="@+id/zoneSpinner"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="5dp"
                android:background="@android:drawable/btn_dropdown" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Main Status Card -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/statusCard"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="20dp"
        app:cardCornerRadius="20dp"
        app:cardElevation="4dp"
        app:layout_constraintBottom_toTopOf="@id/btnPowerOn"
        app:layout_constraintTop_toBottomOf="@id/zoneCard">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="20dp">

            <TextView
                android:id="@+id/statusText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="CHECKING STATUS..."
                android:textColor="@color/text_main"
                android:textSize="36sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/lastSeenText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:text="Updated: --"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/tvReportedBy"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:alpha="0.8"
                android:text="By: System"
                android:textSize="14sp" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Action Buttons -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnPowerOn"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="10dp"
        android:text="POWER HAS RETURNED"
        android:textSize="16sp"
        app:backgroundTint="@color/status_green"
        app:cornerRadius="10dp"
        app:layout_constraintBottom_toTopOf="@id/btnLowVoltage" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnLowVoltage"
        style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="10dp"
        android:text="LOW VOLTAGE WARNING"
        android:textColor="@color/black"
        android:textSize="14sp"
        app:backgroundTint="@color/status_yellow"
        app:cornerRadius="10dp"
        app:layout_constraintBottom_toTopOf="@id/btnPowerOff" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnPowerOff"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="20dp"
        android:text="REPORT POWER CUT"
        android:textSize="16sp"
        app:backgroundTint="@color/status_red"
        app:cornerRadius="10dp"
        app:layout_constraintBottom_toTopOf="@id/btnPumpTimer" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnPumpTimer"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="20dp"
        android:text="⚡ AI IRRIGATION ASSISTANT"
        android:textColor="@color/primary_blue"
        app:layout_constraintBottom_toBottomOf="parent"
        app:strokeColor="@color/primary_blue" />

</androidx.constraintlayout.widget.ConstraintLayout>